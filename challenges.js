//Slice Challengs
//this is from an array (list) that you vefine under a variable (vr)

var names = ['Ben', 'Chloe', 'Devlin', 'Emma'];
console.log(names.slice(2));
// the number above denotes the ammount that will be spliced and not which in the sequence.

console.log(names.slice(2,3));
//ask why it does not reduce the 2nd and 3rd

