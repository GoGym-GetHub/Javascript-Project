/*
let favouriteSongs = [
    "Prince - November Rain",
    "Queen - Fat Bottomed Girls",
    "Ozzy Ozbourne - Changes"
];

favouriteSongs[1] = "Toto - Africa",
favouriteSongs.push("Mettalica - One");

favouriteSongs.pop()
console.log(favouriteSongs)
*/

let favWebsites = [
    "YouTube",
    "Argos",
    "Google"
];

favWebsites.push("gamespot")
favWebsites.push("Yahoo")
favWebsites.pop()
console.log(favWebsites)

//Slice Challengs
//this is from an array (list) that you vefine under a variable (vr)

let names = ["Ben", "Chloe", "Devlin", "Emma"];
console.log(names.slice("3"));
// the number above denotes the ammount that will be sliced and not which in the sequence.
console.log(names.splice("3"));
//ask why it does not reduce the 2nd and 3rd

let Cars = ["Audi", "BMW", "Corvette", "Dodge"]
console.log(Cars)
